# Implementation Issue Feedback (Attempt 1/3)

The previous implementation attempt encountered an issue that requires re-planning.

## Issue Details

HALTED — fundamental conflict inside the PRP (no files were modified).\n\nTHE PROBLEM (at a 10th-grade level):\nThe PRP gives two rules that fight each other, and they cannot both be true after the change.\n\nRule A (Success Criterion #1 + Task 2): After this task, a REAL injected file's FileDetail must NOT carry a `body` field anymore — `hasOwnProperty('body')` must be false. To make that happen, Task 2 deletes the three `body:` pushes from emitText.\n\nRule B (Success Criteria + Anti-Patterns): All the existing REND-* tests must stay green WITHOUT editing the renderer, and the PRP's reasoning is 'REND-* craft body, so the renderer's kept-as-fallback d.body/bodies[i] path handles them.'\n\nThose two rules are fine for most REND-* tests, because most of them hand-build a fake detail object that includes `body`. But ONE test breaks the assumption: REND-11 part (b), in file-injector.test.mjs lines 2699-2716. It does NOT craft a body — it calls the REAL `mod.injectFiles('#@nest.ts')`, grabs `r.details[0]`, and then asserts (lines 2714-2715):\n    assert(typeof d.body === 'string' && d.body.includes('DONE'), ...)\nIn other words, REND-11(b) directly requires that a real injected file STILL carries `d.body`. That is the exact opposite of Rule A. The instant Task 2 removes the body push, this assertion throws, and REND-11 flips red — violating Rule B.\n\nThe PRP's own guard ('If a REND-* case flips, you accidentally edited the renderer — revert') mis-diagnoses this: the renderer is NOT the cause. The renderer fallback (d.body -> bodies[i]) only changes what gets DISPLAYED; it does not add a `body` property to a real FileDetail, and it cannot make `typeof d.body === 'string'` true when emitText stopped setting body. So 'keep body as a deprecated fallback + don't touch the renderer' does NOT save REND-11(b). Only editing REND-11(b) itself would — and that is a test edit the PRP never authorizes (it only authorizes adding ISS2-OFFSET and ISS2-NOBODY; it explicitly says REND-* are 'UNCHANGED' and lists file-injector.test.mjs edits as 'sanity/ASSERTED_EXPORTS + ISS2-OFFSET + ISS2-NOBODY' only).\n\nWHY THIS IS NOT FIXABLE BY FOLLOWING THE PRP LITERALLY:\n- Keeping `body?: string` in the interface (which the PRP demands) does not help — the field stays OPTIONAL, and real emission stops setting it, so `typeof d.body === 'string'` is false for a real file.\n- The renderer fallback (PRP's proposed savior) only affects display text, not the detail object's own properties.\n- The only ways to make BOTH rules hold are (pick one, each violates a different PRP constraint):\n   1. Edit REND-11(b) to assert on contentStart/contentLen + the slice instead of d.body — but the PRP lists REND-* as UNCHANGED and scopes file-injector.test.mjs edits to only sanity/ASSERTED_EXPORTS + the two ISS2 cases. It also splits ownership: P1.M1.T1.S2 owns import-behavior.test.mjs, and the renderer change (which REND-11 is really pinning) is P1.M2.T1.S2.\n   2. Keep setting `body` on real emission for the BUG-1/nested case only — but that contradicts Success Criterion #1 and the whole point of the task (§12.22 no duplicated bytes).\n   3. Have the renderer MUTATE detail objects to backfill body — renderer edits are explicitly forbidden in this task.\n\nSo the PRP's own validation loop ('REND-* must stay green; if one flips you edited the renderer') will fire FALSE-POSITIVE-OR-TRUE-POSITIVE here: REND-11(b) WILL flip, and it is NOT because of a renderer edit — it is because the test pins the old `body` behavior on a real injectFiles call, which this task deliberately removes.\n\nEVIDENCE (exact lines verified by reading the repo, not assumed):\n- file-injector.ts emitText, three body pushes: L894 (`body: content`), L918 (`body: content`), L924 (`body: head`). Task 2 removes all three.\n- file-injector.test.mjs REND-11(b): lines 2690-2716. The conflicting assertion is L2714-2715: `assert(typeof d.body === 'string' && d.body.includes('DONE'), ...)`, run against a REAL `const r = await mod.injectFiles('Review #@nest.ts', [], FIX); ... const d = r.details[0];`.\n- file-injector.ts renderer (L672-673): `const body = typeof d.body === 'string' ? d.body : bodies[i];` — confirms the fallback is display-only and cannot re-add `d.body` to the detail object.\n- Only ONE `.body` assertion exists on a real injectFiles result (grep for `\\.body` in the test file returns exactly 3 hits: L235 comment, L2693 comment, L2714-2715 the assertion). All other body usage is crafted-detail. So REND-11(b) is the single collision point.\n\nWHAT I DID NOT DO (safety):\n- Did not modify file-injector.ts, file-injector.test.mjs, or any other file.\n- Did not touch plan/, PRD.md, or any orchestrator-owned file.\n\nREQUESTED DECISION (the human/orchestrator owns this — I implement, I do not rewrite the plan):\nOption 1 (recommended, smallest): Authorize a one-line test edit to REND-11(b) ONLY — change `assert(typeof d.body === 'string' && d.body.includes('DONE'), ...)` to assert the new §12.22 contract on that same real detail: `assert(d.contentStart != null && d.contentLen != null && r.blocks.join('\\n\\n').slice(d.contentStart, d.contentStart + d.contentLen).includes('DONE'), ...)`. This keeps REND-11's PURPOSE (BUG-1 nested-</file> full-body regression) intact, now backed by offsets instead of body, and is consistent with how ISS2-NOBODY will assert. The crafted-detail REND-11(a) stays unchanged and green via the fallback. This is the natural consequence of the body->offsets migration that the PRP under-specified for REND-11(b).\nOption 2: Explicitly exempt REND-11(b) from 'REND-* unchanged' in this PRP and let P1.M2.T1.S2 (the renderer task, which will also rework the renderer's body->offsets read at L672-673) rewrite REND-11(b) at the same time. Risk: REND-11 is red in the window between this task and P1.M2.T1.S2, so the gate 'node ./file-injector.test.mjs -> 0 failed' cannot pass for THIS task.\nOption 3: Defer this whole task to be sequenced AFTER (or merged with) P1.M2.T1.S2 so the body-removal and the test-retarget land atomically. Cleanest history, but re-sequences the plan.\n\nI can proceed immediately under Option 1 if you confirm; I will not edit REND-* or any test beyond what the PRP authorizes without that confirmation, because the PRP explicitly marks REND-* as UNCHANGED.

## Full Agent Output

Confirmed. There is exactly **one** conflict point: **REND-11(b) at lines 2714-2715** asserts `typeof d.body === "string"` on a real `injectFiles` result. Once Task 2 removes `body:` from emitText, this assertion fails because real injection no longer sets `body`.

This is a **genuine, irreconcilable conflict within the PRP's own stated constraints**:

1. **Success Criterion #1** requires: real injected file → `hasOwnProperty('body') === false`.
2. **Task 2** removes the 3 `body:` pushes from emitText.
3. **Success Criteria + Anti-Patterns** require: "REND-* renderer unit tests stay GREEN (NO renderer edit here)" and "Do NOT touch the renderer."
4. But **REND-11(b)** (an existing test the PRP says must stay green) asserts `typeof d.body === "string"` on a **real** `injectFiles` result — which directly contradicts #1 and #2.

The PRP's claim that "REND-* craft `body`; the renderer's retained fallback handles them" is **only true for REND-11(a) and the other REND-* cases that pass crafted details** — it is **false for REND-11(b)**, which drives the real `injectFiles` and inspects `d.body` directly. The renderer fallback (`d.body` → `bodies[i]`) only affects *display*; it does not change what fields `injectFiles` puts on the detail object.

Per my **Failure Protocol** ("If a fundamental issue with the plan is found, halt and produce a thorough explanation"), I'm halting before making any edits to report this. I have not modified any files.

```json
{
   "result": "issue",
   "message": "HALTED — fundamental conflict inside the PRP (no files were modified).\n\nTHE PROBLEM (at a 10th-grade level):\nThe PRP gives two rules that fight each other, and they cannot both be true after the change.\n\nRule A (Success Criterion #1 + Task 2): After this task, a REAL injected file's FileDetail must NOT carry a `body` field anymore — `hasOwnProperty('body')` must be false. To make that happen, Task 2 deletes the three `body:` pushes from emitText.\n\nRule B (Success Criteria + Anti-Patterns): All the existing REND-* tests must stay green WITHOUT editing the renderer, and the PRP's reasoning is 'REND-* craft body, so the renderer's kept-as-fallback d.body/bodies[i] path handles them.'\n\nThose two rules are fine for most REND-* tests, because most of them hand-build a fake detail object that includes `body`. But ONE test breaks the assumption: REND-11 part (b), in file-injector.test.mjs lines 2699-2716. It does NOT craft a body — it calls the REAL `mod.injectFiles('#@nest.ts')`, grabs `r.details[0]`, and then asserts (lines 2714-2715):\n    assert(typeof d.body === 'string' && d.body.includes('DONE'), ...)\nIn other words, REND-11(b) directly requires that a real injected file STILL carries `d.body`. That is the exact opposite of Rule A. The instant Task 2 removes the body push, this assertion throws, and REND-11 flips red — violating Rule B.\n\nThe PRP's own guard ('If a REND-* case flips, you accidentally edited the renderer — revert') mis-diagnoses this: the renderer is NOT the cause. The renderer fallback (d.body -> bodies[i]) only changes what gets DISPLAYED; it does not add a `body` property to a real FileDetail, and it cannot make `typeof d.body === 'string'` true when emitText stopped setting body. So 'keep body as a deprecated fallback + don't touch the renderer' does NOT save REND-11(b). Only editing REND-11(b) itself would — and that is a test edit the PRP never authorizes (it only authorizes adding ISS2-OFFSET and ISS2-NOBODY; it explicitly says REND-* are 'UNCHANGED' and lists file-injector.test.mjs edits as 'sanity/ASSERTED_EXPORTS + ISS2-OFFSET + ISS2-NOBODY' only).\n\nWHY THIS IS NOT FIXABLE BY FOLLOWING THE PRP LITERALLY:\n- Keeping `body?: string` in the interface (which the PRP demands) does not help — the field stays OPTIONAL, and real emission stops setting it, so `typeof d.body === 'string'` is false for a real file.\n- The renderer fallback (PRP's proposed savior) only affects display text, not the detail object's own properties.\n- The only ways to make BOTH rules hold are (pick one, each violates a different PRP constraint):\n   1. Edit REND-11(b) to assert on contentStart/contentLen + the slice instead of d.body — but the PRP lists REND-* as UNCHANGED and scopes file-injector.test.mjs edits to only sanity/ASSERTED_EXPORTS + the two ISS2 cases. It also splits ownership: P1.M1.T1.S2 owns import-behavior.test.mjs, and the renderer change (which REND-11 is really pinning) is P1.M2.T1.S2.\n   2. Keep setting `body` on real emission for the BUG-1/nested case only — but that contradicts Success Criterion #1 and the whole point of the task (§12.22 no duplicated bytes).\n   3. Have the renderer MUTATE detail objects to backfill body — renderer edits are explicitly forbidden in this task.\n\nSo the PRP's own validation loop ('REND-* must stay green; if one flips you edited the renderer') will fire FALSE-POSITIVE-OR-TRUE-POSITIVE here: REND-11(b) WILL flip, and it is NOT because of a renderer edit — it is because the test pins the old `body` behavior on a real injectFiles call, which this task deliberately removes.\n\nEVIDENCE (exact lines verified by reading the repo, not assumed):\n- file-injector.ts emitText, three body pushes: L894 (`body: content`), L918 (`body: content`), L924 (`body: head`). Task 2 removes all three.\n- file-injector.test.mjs REND-11(b): lines 2690-2716. The conflicting assertion is L2714-2715: `assert(typeof d.body === 'string' && d.body.includes('DONE'), ...)`, run against a REAL `const r = await mod.injectFiles('Review #@nest.ts', [], FIX); ... const d = r.details[0];`.\n- file-injector.ts renderer (L672-673): `const body = typeof d.body === 'string' ? d.body : bodies[i];` — confirms the fallback is display-only and cannot re-add `d.body` to the detail object.\n- Only ONE `.body` assertion exists on a real injectFiles result (grep for `\\.body` in the test file returns exactly 3 hits: L235 comment, L2693 comment, L2714-2715 the assertion). All other body usage is crafted-detail. So REND-11(b) is the single collision point.\n\nWHAT I DID NOT DO (safety):\n- Did not modify file-injector.ts, file-injector.test.mjs, or any other file.\n- Did not touch plan/, PRD.md, or any orchestrator-owned file.\n\nREQUESTED DECISION (the human/orchestrator owns this — I implement, I do not rewrite the plan):\nOption 1 (recommended, smallest): Authorize a one-line test edit to REND-11(b) ONLY — change `assert(typeof d.body === 'string' && d.body.includes('DONE'), ...)` to assert the new §12.22 contract on that same real detail: `assert(d.contentStart != null && d.contentLen != null && r.blocks.join('\\n\\n').slice(d.contentStart, d.contentStart + d.contentLen).includes('DONE'), ...)`. This keeps REND-11's PURPOSE (BUG-1 nested-</file> full-body regression) intact, now backed by offsets instead of body, and is consistent with how ISS2-NOBODY will assert. The crafted-detail REND-11(a) stays unchanged and green via the fallback. This is the natural consequence of the body->offsets migration that the PRP under-specified for REND-11(b).\nOption 2: Explicitly exempt REND-11(b) from 'REND-* unchanged' in this PRP and let P1.M2.T1.S2 (the renderer task, which will also rework the renderer's body->offsets read at L672-673) rewrite REND-11(b) at the same time. Risk: REND-11 is red in the window between this task and P1.M2.T1.S2, so the gate 'node ./file-injector.test.mjs -> 0 failed' cannot pass for THIS task.\nOption 3: Defer this whole task to be sequenced AFTER (or merged with) P1.M2.T1.S2 so the body-removal and the test-retarget land atomically. Cleanest history, but re-sequences the plan.\n\nI can proceed immediately under Option 1 if you confirm; I will not edit REND-* or any test beyond what the PRP authorizes without that confirmation, because the PRP explicitly marks REND-* as UNCHANGED."
}
```

## Instructions

Review this feedback and create a revised PRP that addresses the issue.
If the issue is fundamentally impossible to resolve, output `"result": "fail"` with an explanation.
